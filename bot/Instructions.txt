Dans bot/src/lib/messagingHandlers.js, corriger handleMsgRappelGuerre() et handleMsgRappelRaid() :
Le problème "session expirée" vient du fait que ces handlers stockent les données en mémoire (pendingRappels Map) avec un userId comme clé, mais si l'interaction expire ou que le bot redémarre, la session est perdue.
Fix :

Ajouter deferReply({ ephemeral: true }) en tout premier dans chaque handler
Dans handleMsgRappelGuerre() : fetch les guerriers sans attaque depuis fetchWarData() — utiliser la même logique que checkWarReminders() pour détecter LDC vs GDC
Dans handleMsgRappelRaid() : fetch getRaidSeasons() + getClanMembers() + getClanMembersDR2() pour trouver les membres sans attaque raid
Afficher le preview avec editReply() au lieu de reply()
Les boutons confirmer/annuler utilisent followUp() éphémère

Dans bot/src/setup/sendMessagingPanel.js, vérifier que les boutons "⚔️ Rappel Guerre" et "💎 Rappel Raid" ont bien les bons customIds correspondant aux handlers dans interactionCreate.js.
Dans bot/src/commands/messaging/msg-rappel-raid.js, vérifier que la commande appelle bien handleMsgRappelRaid() et que le handler fonctionne correctement.