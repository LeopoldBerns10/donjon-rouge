Dans bot/src/scheduler.js, deux corrections :

Augmenter la limite d'affichage des membres de 10 à 15 dans buildWarDetailFields()
Appliquer le même fallback LDC pour DR2 que pour DR1 : si getCurrentWar() pour DR2 retourne notInWar ou warEnded, appeler getLdcCurrent() mais chercher le clan DR2 (#CLAN_TAG_DR2) dans les rounds au lieu de DR1. Ajouter dans cocApi.js si nécessaire : getLdcCurrentDR2 ou passer le tag du clan en paramètre.

Le tag DR2 est à trouver dans la config existante ou les variables d'env.